{% extends 'base.html.twig' %}

{% block title %}
    Login
{% endblock %}

{% block body %}
    <div class="container min-vh-100 d-flex justify-content-center align-items-center px-3">
        <div class="w-100" style="max-width: 420px;">
            <div class="border rounded-4 p-4 shadow-sm bg-body text-body">
                <h3 class="text-center mb-4">
                    <i class="bi bi-box-arrow-in-right me-2"></i>
                    <span data-i18n="title.login">User Login</span>
                </h3>

                {% if error %}
                    <div class="alert alert-danger">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3 text-center">
                        Logged in as <strong>{{ app.user.userIdentifier }}</strong>.
                        <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}

                <form method="post" novalidate>
                    <div class="mb-3">
                        <label for="username" class="form-label fw-semibold" data-i18n="login.email">Email</label>
                        <input type="email" value="{{ last_username }}" name="_username" id="username"
                               class="form-control" autocomplete="email" required autofocus
                               data-i18n-placeholder="login.email" placeholder="Enter your email">
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label fw-semibold" data-i18n="login.password">Password</label>
                        <input type="password" name="_password" id="password"
                               class="form-control" autocomplete="current-password" required
                               data-i18n-placeholder="login.password" placeholder="Enter your password">
                    </div>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                    <div class="d-grid mb-3">
                        <button class="btn btn-primary text-white fw-bold" type="submit" data-i18n="login.submit">Sign in</button>
                    </div>

                    <div class="text-center">
                        <small class="text-body-secondary">
                            <span data-i18n="login.no_account">Don't have an account?</span>
                            <a href="{{ path('app_register') }}" class="text-decoration-none" data-i18n="login.register">Register</a>
                        </small>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
