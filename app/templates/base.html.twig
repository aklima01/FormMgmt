<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Homepage{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            padding-top: 70px;
        }
        a {
            text-decoration: none;
        }
        .navbar {
            background-color: #4A90E2;
        }
        .navbar a, .navbar .nav-link {
            color: white !important;
        }
        .tag-btn {
            margin: 4px;
        }
        footer {
            margin-top: 50px;
            font-size: 0.9em;
            text-align: center;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg fixed-top bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="#"><i class="bi bi-house-door"></i> Home</a>

        <!-- Mobile toggler -->
        <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible content -->
        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 flex-row flex-wrap align-items-center">
                <li class="nav-item px-2 border-start border-white d-none d-lg-block"></li>
                <li class="nav-item px-2">
                    <a class="nav-link text-white" href="#"><i class="bi bi-globe2"></i> Templates</a>
                </li>
                <li class="nav-item px-2 border-start border-white d-none d-lg-block"></li>
                <li class="nav-item px-2">
                    <a class="nav-link text-white" href="#"><i class="bi bi-box-arrow-in-right"></i> Login</a>
                </li>
                <li class="nav-item px-2 border-start border-white d-none d-lg-block"></li>
                <li class="nav-item px-2">
                    <a class="nav-link text-white" href={{ path('app_register') }}><i class="bi bi-person-plus"></i> Register</a>
                </li>
            </ul>

            <!-- Search + Language + Theme -->
            <form class="d-flex me-lg-3 my-2 my-lg-0" role="search">
                <input class="form-control me-2" type="search" placeholder="search any keywords">
                <button class="btn btn-outline-light" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </form>

            <div class="d-flex align-items-center text-white">
                <span class="me-2">En</span>
                <div class="border-start mx-2 d-none d-lg-block" style="height: 1.5rem;"></div>
                <i class="bi bi-moon" style="cursor: pointer;"></i>
            </div>
        </div>
    </div>
</nav>






<div class="container">
    {% block body %}{% endblock %}
</div>

<footer class="text-muted mt-5">
    <p>© {{ "now"|date("Y") }} – FormMgmt</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
